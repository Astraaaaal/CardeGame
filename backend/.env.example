# ── Database ──
DATABASE_URL=postgresql+asyncpg://cardegame_user:password@localhost:5432/cardegame

# ── JWT ──
JWT_SECRET=change-me-to-a-random-secret-key
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=30

# ── Bcrypt ──
BCRYPT_ROUNDS=12

# ── Admin ──
ADMIN_KEY=une-cle-longue-et-aleatoire
ENVIRONMENT=development

# ── Cloudinary ──
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# ── CORS ──
CORS_ORIGINS=["http://localhost:5173"]

# ── URL publique du jeu (liens des e-mails, retour de paiement) ──
PUBLIC_APP_URL=http://localhost:5173

# ── E-mails / newsletter (Brevo) — vide = e-mails écrits dans les logs ──
BREVO_API_KEY=
BREVO_NEWSLETTER_LIST_ID=
EMAIL_SENDER_ADDRESS=no-reply@cardegame.local
EMAIL_SENDER_NAME=CardeGame

# ── Paiements (Stripe) — vide = achats en euros refusés ──
# Clé restreinte recommandée (rk_test_… / rk_live_…) avec « Checkout Sessions : écriture ».
# En local, le secret du webhook est affiché par : stripe listen --forward-to localhost:8000/api/premium/stripe-webhook
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
